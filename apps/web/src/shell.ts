export function renderLingotorteShell(): string {
  return `
    <section class="lingotorte-shell" aria-labelledby="lingotorte-title">
      <p>Local-first study workspace</p>
      <h1 id="lingotorte-title">Lingotorte</h1>
      <article class="lingotorte-card">
        <h2>P0 skeleton</h2>
        <p>Import owned/local media and explicit subtitle files, keep providers disabled by default, and preserve source context for every future saved item.</p>
      </article>
    </section>
  `;
}
